# ServiceNow ticket MCP tools Deployment + Service.
# Image: servicenow-mcp:dev (local tag for `ctr images import` on the k3s node).
# Intended published name: ghcr.io/sebbycorp/servicenow-mcp:dev
# imagePullPolicy: IfNotPresent — the pod cannot start until the image is
# imported on Viper. See images/servicenow-mcp/README.md.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicenow-mcp
  namespace: kagent
  labels:
    app.kubernetes.io/name: servicenow-mcp
    app.kubernetes.io/part-of: service-now-sandbox-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: servicenow-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: servicenow-mcp
        app.kubernetes.io/part-of: service-now-sandbox-agent
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: servicenow-mcp
          image: servicenow-mcp:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: mcp
              containerPort: 8084
              protocol: TCP
          env:
            - name: SERVICENOW_HOST
              valueFrom:
                secretKeyRef:
                  name: servicenow-mcp
                  key: SERVICENOW_HOST
            - name: SERVICENOW_USERNAME
              valueFrom:
                secretKeyRef:
                  name: servicenow-mcp
                  key: SERVICENOW_USERNAME
            - name: SERVICENOW_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: servicenow-mcp
                  key: SERVICENOW_PASSWORD
            - name: SERVICENOW_TLS_VERIFY
              value: "true"
            - name: MCP_HOST
              value: "0.0.0.0"
            - name: MCP_PORT
              value: "8084"
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /health
              port: mcp
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: mcp
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: servicenow-mcp
  namespace: kagent
  labels:
    app.kubernetes.io/name: servicenow-mcp
    app.kubernetes.io/part-of: service-now-sandbox-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: servicenow-mcp
  ports:
    - name: mcp
      port: 8084
      targetPort: mcp
      protocol: TCP
